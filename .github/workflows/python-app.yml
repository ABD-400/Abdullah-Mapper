name: Build Abdullah Mapper
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build with Buildozer
        uses: Enteee/buildozer-action@v1.2.2
        with:
          buildozer_version: stable
          workdir: .
